# Skills Policy Canonical Notes

- Default load mode: `subagent-first`.
- Enforce per stage via `skills.<stage>.enforce_required`.
- Legacy fallback uses `skills.enforcement` when stage key is absent.
